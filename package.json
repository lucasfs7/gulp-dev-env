{
  "version": "0.1.0",
  "scripts": {
    "start": "http-server ./build -i -o --cors",
    "watch": "gulp",
    "compile": "gulp build",
    "test": "gulp compile:templates && browserify -t liveify --extension=.ls test/*.ls | testling"
  },
  "dependencies": {
    "browser-request": "^0.3.2",
    "express": "4.14.0"
  },
  "devDependencies": {
    "browserify": "13.1.0",
    "browserify-livescript": "0.2.3",
    "gulp": "3.9.1",
    "gulp-concat": "2.6.0",
    "gulp-define-module": "0.1.4",
    "gulp-imagemin": "3.0.3",
    "gulp-jade": "1.1.0",
    "gulp-stylus": "2.5.0",
    "gulp-util": "3.0.7",
    "http-server": "^0.9.0",
    "vinyl-source-stream": "1.1.0"
  }
}
